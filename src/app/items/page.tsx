"use client";

import type { NextPage } from "next";

const Page: NextPage = () => {
	return (
		<main className="flex h-full w-full flex-col items-center justify-center">
			<h1 className="text-6xl font-bold">Items</h1>
		</main>
	);
};

export default Page;
